# §17 验收清单核对记录（M5/M6 完成后）

> 日期：2026-09-05　环境：Windows 10 / Python 3.12.6 (venv312) / Electron 33.4.11
> 依据：docs/顶层设计-MCP化与桌面端.md §17

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| 1 | 无 Python 的目标机器可安装并启动 | ✅*/⏳ | `pnpm dist` 已产出 `desktop/release/内容收集工作台 Setup 0.1.0.exe`（150MB，NSIS，extraResources 含 backend/frontend/venv312/app.py）；打包版运行时启动验证通过：2 秒内后端+MCP 就绪、SPA 加载、45 工具 MCP 调用成功、`state/` 隔离。**残留**：干净机器（无基础 Python）验证需另机执行——打包 venv 依赖 pyvenv.cfg 指向的基础解释器 |
| 2 | 5200/3333 被占用时有明确行为与诊断 | ✅ | ServiceManager 冒烟：占用时 `EXPECTED_FAIL: 后端启动失败：请检查…端口占用`，不进入重启循环 |
| 3 | 后端异常退出后不重复启动调度器 | ✅ | 调度器为后端进程内单例；Electron 单实例锁 + 单 ManagedService 实例；重启走退避预算（§15）。打包版实测：backend 退出后按退避重启且错误入日志 |
| 4 | 任务执行中退出应用，重开状态可解释 | ✅ | `task_manager.recover()` 单测：running→interrupted，有 cursor 标记 resumable |
| 5 | 同一采集请求重试不重复产出 | ✅ | 幂等键单测 + 内容 hash 去重单测（同身份同内容 → deduped=True） |
| 6 | 批量任务部分失败可查询、可选择性重试 | ✅* | 分条目 items 记录可查询；"选择性重试"以重提交对应 URL 实现（去重保证安全），无独立重试工具（记录为后续优化） |
| 7 | 二维码过期/取消/客户端不支持图片均有路径 | ✅ | 过期/取消/刷新预算单测；图片不可渲染时的兜底 = Web 仪表盘"待认证"区渲染同一 AuthRequest |
| 8 | 磁盘写满不留完整产物 | ✅(设计保证) | `.tmp` → `_COMPLETE` → os.replace 原子提交协议；写满时临时目录不发布（未做人为写满注入测试） |
| 9 | 非授权客户端无法调用本地业务接口 | ✅ | 新端点：令牌/同源双因子（403/401 单测+实测）；全局：Host 校验（evil host 403 实测）+ CORS 白名单（evil origin 无 ACAO 实测）。**打包版实测**：dev 令牌访问打包后端 401（实例隔离生效）。**残留**：legacy 平台 GET 端点未挂令牌（CORS 白名单已覆盖跨域读取） |
| 10 | 恶意 HTML/异常文件名/路径穿越不突破边界 | ✅ | 预览 iframe `sandbox=""` + CSP `default-src 'none'; sandbox`；条目文件服务仅限 metadata.files 白名单（穿越 403 实测）；目录名经 `_safe_slug` 清洗 |
| 11 | 日志与诊断包不包含凭证 | ✅ | 双层脱敏：Electron 捕获层（logger.js `redact()`，覆盖子进程全部 stdout/stderr）+ Python logging 过滤器（`backend/core/log_redact.py` RedactFilter，runtime 启动时挂载）。规则与单测：Cookie/Bearer/token/sessionid/URL 参数/uuid 掩码（保留键名+前 4 字符），6 项测试通过 |
| 12 | 升级/卸载不误删用户内容与账号数据 | ✅(配置) | NSIS 配置 `deleteAppDataOnUninstall: false`；`state/` `output/` `data/` 与代码目录分离（.gitignore 同步）；打包版实测数据写入独立 `resources/service/state/`。安装器实际升级/卸载流程待真机安装验收 |

## 打包验证补充（本轮）

- **产物**：`内容收集工作台 Setup 0.1.0.exe`（150MB）+ blockmap + win-unpacked；
- **运行时启动**：打包版 exe 启动 → 监督器拉起打包 venv 内后端(5200)与 MCP(3333)，2 秒就绪；SPA 标题正确；45 工具 MCP 调用成功；实例令牌隔离（dev 令牌 401）；`resources/service/state/` 数据隔离；
- **过程中发现并修复的打包 bug**：app.py/mcp_server.py 未包含在 extraResources（首启即败，监督日志准确记录并退避重启——反向验证了诊断能力）；
- **winCodeSign 符号链接特权问题**：以预置缓存目录（`Cache/winCodeSign/winCodeSign-2.6.0`）绕过，缺失的 2 个 darwin symlink 仅影响 macOS 签名，与 win 构建无关。

## 本轮新增验证

- **M5 端到端**：`/api/status` 聚合（后端/MCP 探测/调度器/FFmpeg/库计数）；内容库受限文件服务（manifest 内 200 + text/markdown，穿越/白名单外 403）；SPA 四区导航与三个新页面资源全部 200。
- **M6 端到端**：MCP 工具总数 40（新增 23 个平台工具）；`bili_detect_url` 真实调用返回 B站视频数据（author/cover/pages）；`douyin_task_status` 正常。
- **安全补强**：`CORS(app)` 默认全开 → 白名单正则；全局 `before_request` Host 校验。均经新鲜进程实测。
- **回归**：`tests/test_core_contracts.py` 28/28 通过（含上述改动后复跑）。

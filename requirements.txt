flask>=3.0
flask-cors>=4.0
requests>=2.31
curl_cffi>=0.7
cryptography>=42.0
playwright>=1.40
scrapling>=0.2
pysocks>=1.7
pywebview>=5.0
pythonnet>=3.0; sys_platform == "win32"
clr_loader>=0.2; sys_platform == "win32"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-WebKit>=10.0; sys_platform == "darwin"
xhshow>=0.2.0
pyyaml>=6.0
httpx
mitmproxy>=12.0
# mitmproxy<=12.2.3 把 typing-extensions 锁在 <=4.14;固定该版本避免与 pydantic 的依赖解析冲突
typing-extensions==4.14.0

beautifulsoup4>=4.12
markdown>=3.5

# 登录二维码本地渲染(SVG)
segno>=1.5
